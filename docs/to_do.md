T
- When colliding against ENTITY, add a short offset to direction (poke)
- add gobblelin enemy
- I was too eager and held down the "adjust tie" button while still sliding. Which it doesn't register, so you're at best just turning your character once you stop.
- Implement sfx
- Implement music via music director (plays track for the first time when happens, but keeps it in the background when leaving context)
